// Consumed by consumer.ts via the named re-export in barrel.ts -> NOT flagged.
// (Characterizes: a named re-export through a barrel credits the original source.)
export function reExported(): void {}
