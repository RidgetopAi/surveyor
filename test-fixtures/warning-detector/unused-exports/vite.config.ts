// A build-tool config file. CONFIG_FILE_PATTERNS matches `vite.config.ts`, so
// every export here is treated as a framework convention -> NOT flagged,
// even though nothing imports it. (Characterizes the config-file skip.)
export default {
  plugins: [],
};
