/**
 * Resolver module - shared import resolution utilities
 */

export {
  resolvePathAlias,
  normalizeImportSource,
  normalizeFilePath,
  getNormalizedPaths,
  buildFilePathIndex,
  resolveImportToFileId,
  getFileNodes,
} from './import-resolver.js';
