{
  "name": "surveyor",
  "version": "0.1.0",
  "private": true,
  "description": "Codebase mapping and visualization tool",
  "author": "Brian",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @surveyor/ui dev",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "prettier": "^3.9.0",
    "typescript": "^5.9.3"
  }
}
